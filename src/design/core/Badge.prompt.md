Badge — bracketed status tag, Skein's substitute for an icon system: renders claim/edge state as text, e.g. `[ACTIVE]`.

```jsx
<Badge status="active">ACTIVE</Badge>
<Badge status="superseded">SUPERSEDED</Badge>
<Badge status="correction">CORRECTION · CURRENT</Badge>
```
