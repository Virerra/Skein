QueryBar — terminal-style input that makes the RAG retrieval layer visually concrete: types like a shell command, returns a monospace result line.

```jsx
<QueryBar value={q} onChange={e=>setQ(e.target.value)} result='3 hops · chain · current: Postgres' />
```
